server:
  port: ${SCENARIO_PORT:8282}

spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:56379}
      # Fail fast when the cache is down: default Lettuce command timeout is 60 s,
      # which would hang every what-if run. The controller degrades to no-cache.
      connect-timeout: 2000
      timeout: 2000
  datasource:
    url: ${POSTGRES_JDBC:jdbc:postgresql://localhost:55432/gaia}
    username: ${POSTGRES_USER:gaia}
    password: ${POSTGRES_PASSWORD:gaia_dev}
    driver-class-name: org.postgresql.Driver
    hikari:
      # never block startup on an unavailable PostGIS (Redis stays the source of truth)
      initialization-fail-timeout: -1
      connection-timeout: 3000
      maximum-pool-size: 4

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus